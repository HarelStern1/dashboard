import styled from "@emotion/styled";
import { Box } from "@mui/material";

export default {
  RowsWrapper: styled(Box)(() => ({
    display: "flex",
    flexDirection: "column",
    gap: "20px",
  })),
};
